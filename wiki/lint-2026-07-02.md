---
type: audit
title: Full-Wiki Lint & Improvement Pass (2026-07-02)
description: A whole-wiki review (mechanical checks + four parallel content audits over ~96 pages) — what was found, what was fixed in place, and what was deliberately left flagged-but-unedited.
tags: [meta, audit, lint]
timestamp: 2026-07-02T00:00:00Z
status: stable
sources: []
---

# Full-Wiki Lint & Improvement Pass (2026-07-02)

> **As of 2026-07-02.** Unlike the [2026-06-22 verification-flags audit](verification-flags-2026-06-22.md)
> (corrections recorded *without* editing the audited pages), this pass **applied fixes in place** —
> each with an inline `lint correction 2026-07-02` flag where a number changed — and records here what
> was fixed vs deliberately left. Scope: all 96 wiki pages + CLAUDE.md + tooling outputs.

## What was checked, and found CLEAN
- **Links:** 3,893+ relative links — all resolve (re-run after every fix batch).
- **Frontmatter:** `type`/`status`/`timestamp`/`description` valid on all pages.
- **Orphans:** none. **log.md grammar:** 45/45 entries conform to `## [date] <op> | <title>`.
- **Quant content:** Kelly formulas, leveraged-ETF whipsaw algebra, expectancy, CVaR/VaR coherence,
  DSR logic, Odean/Barber-Odean figures, Kaminski-Lo stop-loss regime logic — all correct and
  correctly attributed.
- **Sources area:** index coverage complete (25/25); the "~17% implied move" refutation correctly
  carried everywhere; the earlier fundamental-cycle briefs vs later positioning briefs are
  complementary axes, **not** a contradiction; all "Feeds/Links into the wiki" targets reciprocate.
- **As-of discipline:** every market-note and note carries a prominent as-of flag.
- **Jun-23 event data** (KOSPI −9.99%, MU −13.18%→$1,051.77, ~$3.8B foreign selling, JPM ~$165B,
  BofA ~3%/~5% trigger) is consistent across all six pages that cite it.

## Fixed in place (this pass)
1. **Micron FQ3 page post-mortemed** ([page](market-research/memory/micron-fq3-2026-earnings.md)) —
   the top finding. The page was frozen in preview state 8 days after its catalyst resolved: stub
   unfilled, `status` not flipped, and its directional lean ("tilts toward the AVGO outcome") resolved
   *against* it with no in-place flag — propagating a live cross-page contradiction ("only CY2026 sold
   out" vs siblings' "sold out through CY2027"). Filled the post-mortem (actuals, tape, scenario grade,
   swing-magnitude grade, aftermath), marked the scenario table resolved, added ⛳ resolution flags at
   both stale claim sites, flipped `status: stable`, expanded Relationships (post-mortem note, both
   positioning pages, monitoring system, week-ahead).
2. **week-ahead-2026-06-22 post-mortem filled** (PCE hot; Micron beat-and-raise; the week's defining
   event — the Jun-23 panic — wasn't on its calendar; forward link to the Jun-29 forensic diagnosis).
3. **Note corrections (inline-flagged):** the stop-vs-hold note's **"GM guided ~68%" → ~81%** (the 68%
   had already been recorded as erroneous in the log) + an unverified-flag on "Q4 guided ~$50B" + a
   consensus-baseline caveat ($19.72/$34.52B pre-print vs $20.49/$35.69B at-print); the rotation note's
   **"+986% 1Y" → +756%** (the verified figure); the rotate-for-momentum note's "pop is gone" got a
   post-close addendum (MU V-reversed to close **+1.14% $1,145.28** that session).
4. **Methodology errors:** swing-trading's "~1-2 orders of magnitude" cost claim → **~3-4×** (its own
   numbers: 61-76 vs 200-270 bps); McLean-Pontiff decay harmonized across three pages (**~26% OOS /
   ~58% post-publication, ~32pp attributable to crowding** — the "~35%" wording removed);
   leveraged-etf-decay's trend example → **+69% vs naive +63% (+6 pts)** (was +60%/+9 pts, mixing
   simple and compounded conventions); its mislabeled "drift ~zero" link retargeted to the swing brief.
5. **Stale labels/metadata:** wiki/index.md (brief count 19→23, Last-ingest 06-24→06-30, synthesis
   one-liner, added an Audits section); ml-stats/index's "(stub)" label on the fleshed-out
   regime-detection page; market-research/index (concatenated Micron/SK-Hynix bullets split; Micron
   description now shows the resolution); frontmatter timestamps bumped where dated update blocks
   post-dated them (supply-demand-cycle, market-research index); cyclical-vs-new-era's body as-of line
   now names its update dates.
6. **Cross-page reconciliation flags** (unresolved numbers now point at each other instead of silently
   coexisting): hyperscaler capex **~$602B Big-Five vs $725B broader bucket** (semiconductors ↔ H2
   playbook); record levered-ETF AUM **~$203B vs ~$218B** (Jun-29 ↔ Jun-30 positioning pages); the
   apparent **$1,255 vs $1,211 ATH** conflict clarified on conviction-swing (Jun-22 ATH close vs the
   Jun-25 post-print intraday spike — different marks, no conflict).
7. **Missing cross-links added:** limits-to-arbitrage → disposition-effect; momentum → stop-losses
   (+ resolved its "crash magnitude not verified" flag via regime-detection's verified Daniel-Moskowitz
   figures); regime-detection → stop-losses backlink; monitoring-system → the Jun-30 unwind-duration
   page (its missing Layer-2 duration companion); volatility-smile-dynamics got a Relationships section.
8. **Graduation debt cleared — three new pages** (the most-pointed-at gaps, 4+ inbound references):
   [Navigating Nonlinear Markets](synthesis/navigating-nonlinear-markets.md) (`overview` — robustness
   over prediction), [Mispricing & Expectations](shared/concepts/mispricing-and-expectations.md) and
   [Who Sets Price](shared/concepts/who-sets-price.md) (`concept`) — all wired into indexes, source
   notes' graduation footers, and Relationships backlinks.
9. **Stub built out:** [risk-of-ruin](shared/concepts/risk-of-ruin.md) (the survival pillar 8+ pages
   cite) — gambler's-ruin formula, Thorp/MacLean-Ziemba drawdown-probability under fractional Kelly
   (P ≈ x^(2/k−1)), the forced-seller ruin mode, fat-tail caveats; `status` → active.
10. **Books' missing Caveats sections** added (Cartea et al.; Sinclair). **Schema:** `type: index`
    documented in CLAUDE.md §4 (was in use on every index page but absent from the vocabulary).
    **Housekeeping:** `.claude/` (local session state) added to `.gitignore`.

## Flagged but deliberately NOT edited
- **Frozen source briefs** keep their as-run figures (e.g. IV ~103% in the pre-print Micron briefs,
  corrected to ~155% front-week downstream) — run records are immutable; the curated pages carry fixes.
- **log.md ordering** is non-monotonic near the end (a 06-29-dated entry appended between two 06-30
  entries) — append-only wins over date order; noted, not reordered.
- **`semiconductors.md` "MU ~+762% TTM"** stock-return/EPS-growth conflation — already documented in
  [verification-flags §6](verification-flags-2026-06-22.md); left per that audit's convention.
- **SK Hynix and MU both "~$1.28T"** market cap — each internally derived; plausibly a genuine
  coincidence; re-verify on the next Korea-names refresh.
- **SKEW 143 vs 141.85** for the same Jun-23 print (positioning pages) — sub-1% wobble, different
  sourcing; noted only.
- **Sources heading drift** ("Feeds into the wiki" ×~18 vs the template's "Links into the wiki" ×6;
  "Key takeaways" literal on only 3) and **`resource:` convention drift** (absent vs empty-dir vs
  `workflow wf_…`) — substance present everywhere; standardize opportunistically on future edits.
- **data-integrity.md** remains a (reasonable) stub — expansion is a real open item, not an error.

## Open follow-ups this audit generates
- **Jul-2 jobs post-mortems** (due after today's print): grade the Jun-29 bounce-vs-second-leg call,
  the Jun-30 ~2-wk-acute/~1-2-wk-tail call, and the week-ahead-06-29 page.
- Reconcile the **hyperscaler-capex scopes** ($602B vs $725B) and **levered-ETF AUM** ($203B vs $218B)
  next time a positioning/capex run happens.
- Expand **data-integrity.md**; consider the notes' remaining graduation candidates (conviction-swing
  worked-example; "where to spend effort" section in trading-system-fundamentals).
